name: <unique name>
interval: <cron, e.g. "0 * * * *">
alert_mute_interval: <hours>
load:
    treshold: <load>
    hysteresis: <load>
    samples: <number>
ram:
    treshold: <percent>
    hysteresis: <percent>
    samples: <number>
swap:
    treshold: <percent>
    hysteresis: <percent>
    samples: <number>
temperature:
    sensor: <path, e.g. /sys/class/thermal/thermal_zone0/temp>
    treshold: <percent>
    hysteresis: <percent>
    samples: <number>