name: <unique name>
check_interval: <cron, e.g. "0 * * * *">
report_interval: <cron, e.g. "0 0 * * *">
aggregation: <hours>
binary: <smartctl binary file>
database: <file>
global_limits:
    <attribute id>:
        evaluation: <max/min/delta_max/delta_min>
        value: <value>
drives:
    <identifier>:
        <attribute id>:
            evaluation: <max/min/delta_max/delta_min>
            value: <value>
alias:
    <identifier>: <alias>
blacklist:
    - <identifier>
