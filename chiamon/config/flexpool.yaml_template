name: <unique name>
summary_interval: <cron, e.g. "0 * * * *">
check_interval: <cron, e.g. "0 * * * *">
connection_error_mute_interval: <hours>
connection_error_tolerance: <number>
connection_retry: <number>
alert_mute_interval: <hours>
alert_tolerance: <number>
address: <XCH wallet address>
currency: <string, e.g. "USD">
worker_whitelist: <list of worker names or null>