summary_intervall: <cron, e.g. "0 * * * *">
check_intervall: <cron, e.g. "0 * * * *">
connection_error_mute_intervall: <hours>
alert_mute_interval: <hours>
address: <XCH wallet address>
currency: <string, e.g. "USD">
check_workers: <list of worker names or "all">