name: <unique name>
summary_interval: <cron, e.g. "0 * * * *">
check_interval: <cron, e.g. "0 * * * *">
alert_mute_interval: <hours>
cert: <file>
key: <file>
host: <ip:port, usually 127.0.0.1:9256>
wallet_id = <id>